<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stylefeng.guns.rest.common.persistence.dao.MtimePromoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.stylefeng.guns.rest.common.persistence.model.MtimePromo">
        <id column="uuid" property="uuid" />
        <result column="cinema_id" property="cinemaId" />
        <result column="price" property="price" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="status" property="status" />
        <result column="description" property="description" />
    </resultMap>

    <select id="queryPromosByCinemaId" parameterType="java.lang.Integer" resultType="com.stylefeng.guns.api.promo.vo.PromoVO">
      SELECT
	    p.uuid AS uuid,
	    p.price AS price,
	    p.start_time AS startTime,
	    p.end_time AS endTime,
        p.status AS status,
	    s.stock AS stock,
	    p.description AS description,
	    c.UUID AS cinemaId,
	    c.cinema_name AS cinemaName,
	    c.img_address AS imgAddress,
        c.cinema_address AS cinemaAddress

      FROM
	    mtime_promo p
	  LEFT JOIN mtime_promo_stock s ON s.promo_id = p.uuid
	  LEFT JOIN mtime_cinema_t c ON c.UUID = p.cinema_id
      <where>
          p.`status` = #{promoStatus}
          <if test="cinemaId != null and cinemaId != ''">
              and p.cinema_id = #{cinemaId}
          </if>
      </where>
    </select>


</mapper>
